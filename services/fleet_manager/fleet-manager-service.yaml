apiVersion: v1
kind: Service
metadata:
  name: fleet-manager-service
  namespace: default
spec:
  selector:
    app: fleet-manager
  ports:
    - port: 50001
      targetPort: 50001
      name: grpc
  type: ClusterIP  # Only exposed inside the cluster