syntax = "proto3";

package user_common;
option go_package = "team_dynamics/api/proto/user_common";

message ExternalKey {
    oneof key {
        int64 steam_id = 1;
    }
}

message UserData {
    optional int64 id = 1;
    optional string name = 2;
}
