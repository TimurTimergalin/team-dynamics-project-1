apiVersion: v1
kind: Service
metadata:
  name: user-storage-service
  namespace: user
spec:
  selector:
    app: user-storage
  ports:
    - port: 5432
      targetPort: 5432
      name: user-storage
  type: ClusterIP
