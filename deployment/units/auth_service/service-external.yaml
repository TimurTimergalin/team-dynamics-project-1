apiVersion: v1
kind: Service
metadata:
  name: auth-service-external
spec:
  selector:
    app: auth-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
      appProtocol: k8s.timeweb.cloud/proto-http
  type: LoadBalancer
