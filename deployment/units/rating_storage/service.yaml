apiVersion: v1
kind: Service
metadata:
  name: rating-storage-service
spec:
  selector:
    app: rating-storage
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  type: ClusterIP
