apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    environment: dev
  name: rating-storage-pvc
  namespace: rating
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
