apiVersion: v1
kind: Service
metadata:
  name: match-history-storage-service
  namespace: match-history
spec:
  selector:
    app: match-history-storage
  ports:
    - port: 5432
      targetPort: 5432
      name: match-history-storage
  type: ClusterIP
