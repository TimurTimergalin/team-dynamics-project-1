apiVersion: autoscaling.agones.dev/v1
kind: FleetAutoscaler
metadata:
  name: tag-duels-autoscaler
spec:
  fleetName: fleet1
  policy:
    type: Buffer
    buffer:
      bufferSize: 1
      minReplicas: 0
      maxReplicas: 10