apiVersion: v1
kind: Service
metadata:
  name: user-service-external
spec:
  selector:
    app: user-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
      appProtocol: k8s.timeweb.cloud/proto-http
  type: LoadBalancer